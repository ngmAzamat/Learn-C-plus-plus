#include <iostream>
#include <string>

using namespace std;
struct tree {
    int ages;
    float height;
    bool is_alive;
    string name;
}


int main() {
    tree.dub;
    dub.name = "dub";
    dub.is_alive = true;
    tree.Not_dub;
    dub.name = "Not_dub";
    dub.is_alive = true;
    return 0;
}